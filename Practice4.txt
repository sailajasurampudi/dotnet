using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.IO;
public class AddimissionProcess{
	public float ScorePer {get; set;}
	public int ConductRating {get;set;}
	public AddimissionProcess(float score, int rt){
		ScorePer=score;
		ConductRating=rt;
	}
}
public class AddmissionRequirement{
	float minscoreperc {get; set;}
	int minconductrating {get; set;}
	public AddmissionRequirement(float per,int rating){
		minscoreperc=per;
		minconductrating=rating;
	}
}
public class Admit{
	public static void Main(){
		AddimissionProcess adp=new AddimissionProcess(50.0f,7);
		AddmissionRequirement adr=new AddmissionRequirement(60.0f,3);
		try{
			if(CheckScore(adp.ScorePer)&&CheckRating(adp.ConductRating)){
				Console.WriteLine("Addmission Granted");
				
			}
			else
			{
				throw new AplogoyException("Sorry You are not eligible ");
			}
		}
		catch (AplogoyException e){
			Console.WriteLine(e.Message);
		}
	}
	class AplogoyException: Exception{
		public AplogoyException(string message):base(message){
		}
	}
	public static bool CheckScore(float scr){
		bool b;
		if(scr>60){
			b=true;
		}
		else
		b=false;
		return b;
	}
	public static bool CheckRating(int rt){
		bool b;
		if(rt>3){
			b=true;
		}
		else
		b=false;
		return b;
	}
}